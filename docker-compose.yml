version: '3.8'

services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      # Используем переменные окружения из Jenkins
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-lk_case2_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      # Проверка готовности БД
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-lk_case2_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: lk-case2-backend
    restart: always
    ports:
      # Проброс порта для API
      - "${PORT:-8999}:${PORT:-8999}"
    environment:
      # Настройки подключения к БД. 'db' - имя сервиса.
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-lk_case2_db}
      ASPNETCORE_URLS: http://+:${PORT:-8999}
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: lk-case2-frontend
    restart: always
    ports:
      # Фронтенд будет доступен на порту 80 хоста
      - "80:80"
    depends_on:
      backend:
        condition: service_started

volumes:
  postgres_data:
